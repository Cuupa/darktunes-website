import { describe, expect, it } from 'vitest'
import { EpkPageBackgroundLayer } from './EpkPageBackgroundLayer'

describe('EpkPageBackgroundLayer', () => {
  it('exports component', () => {
    expect(EpkPageBackgroundLayer).toBeTypeOf('function')
  })
})
