import { describe, expect, it } from 'vitest'
import { EpkColorThemePicker } from './EpkColorThemePicker'

describe('EpkColorThemePicker', () => {
  it('exports component', () => {
    expect(EpkColorThemePicker).toBeTypeOf('function')
  })
})
