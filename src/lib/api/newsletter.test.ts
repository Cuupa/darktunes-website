/**
 * DEPRECATED: Tests for newsletter.ts (newsletter feature moved to Shopify)
 * 
 * These tests are kept temporarily but will be removed once all newsletter
 * routes are updated to use Shopify instead.
 */

import { describe, it, expect } from 'vitest'

describe('newsletter (deprecated)', () => {
  it('placeholder test', () => {
    expect(true).toBe(true)
  })
})
